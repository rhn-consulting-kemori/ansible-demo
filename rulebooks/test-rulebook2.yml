---
- name: Hello Events
  hosts: localhost

  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000

  rules:
    - name: Data send
      condition: event.payload.datatype == "trouble"
      action:
        run_job_template:
          name: test_playbook2
          organization: Default
          job_args:
            issue: event.payload.subject