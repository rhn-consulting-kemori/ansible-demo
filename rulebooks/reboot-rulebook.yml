- name: Trouble Recover Reboot Rulebook
  hosts: localhost

  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5002

  rules:
    - name: Reboot Sample-Apl
      condition: event.payload.request == "reboot"
      actions:
        - run_job_template:
            name: Sample-apl Start
            organization: Default

        - run_job_template:
            name: Redmine Issue Update With Description
            organization: Default
            job_args: 
              extra_vars: 
                ticket_no: '"{{ event.payload.ticket_no }}"'
                status_id: '"{{ event.payload.status_id }}"'
                context: "2"
                action_memo: "再起動で対応"

         - run_job_template:
            name: Teams Complete Notification
            organization: Default
            job_args: 
              extra_vars: 
                ticket_no: '"{{ event.payload.ticket_no }}"'