---
- name: Error Recieve Rulebook
  hosts: localhost

  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000

  rules:
    - name: Error Recieve
      condition: event.payload.item.system_name is defined 
      action:
        run_job_template:
          name: test_playbook3
          organization: Default
          job_args:
            extra_vars:
              system_name: "{{ event.payload.item.system_name }}"
              timestamp: "{{ event.payload.item.timestamp }}"
              err_id: "{{ event.payload.item.err_id }}"
              err_target: "{{ event.payload.item.err_target }}"
              err_code: "{{ event.payload.item.err_code }}"
              context: "{{ event.payload.item.context }}"
              memo: "{{ event.payload.item.memo }}" 