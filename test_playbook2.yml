---
- name: debug msg play
  hosts: localhost
  gather_facts: false

  tasks:
    - name: debug project_id
      vars:
        data: "{{ issue | from_json }}"
      ansible.builtin.debug:
        msg: "{{ data.system_name }}-{{ data.timestamp }}"